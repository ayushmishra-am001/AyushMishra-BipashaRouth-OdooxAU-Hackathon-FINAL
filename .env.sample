# Sample environment variables for development
# Copy this to .env and fill with real values (never commit secrets)

DATABASE_URL=postgres://user:password@localhost:5432/rms_dev
JWT_SECRET=replace_this_with_a_secure_random_string
PORT=5000
ADMIN_SIGNUP_KEY=change_me
